/**
 * Tailwind CSS v4 Configuration
 *
 * NOTE: In Tailwind v4, configuration has moved to CSS using the @theme directive.
 * This file is kept for backward compatibility and tooling support but is NOT
 * the primary source of configuration.
 *
 * Primary configuration is in: src/index.css using @theme { }
 *
 * Tailwind v4 automatically detects content files - no content array needed!
 */

/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind v4 has automatic content detection
  // No need to specify content array anymore

  // Theme configuration moved to @theme directive in src/index.css
  // This minimal config is kept for tooling compatibility
}
